import {StyleSheet} from 'react-native';
import Colors from '../../../shared/theme/Colors';

export default StyleSheet.create({
  // chats: {flex: 1, justifyContent: 'center', alignItems: 'center'},
  chats: {
    flex: 1,
  },

  chat: {
    flex: 1,
  },
});
